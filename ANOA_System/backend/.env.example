# Example env for ANOA backend
# Generate a strong secret and set it in your environment or a real .env (do NOT commit secrets to git)
ANOA_INTERNAL_KEY=replace_this_with_a_strong_random_key
GEMINI_API_KEY=
DEMO_MODE=false
